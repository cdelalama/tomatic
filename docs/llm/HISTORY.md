# LLM Session History

Append-only record of meaningful LLM-assisted work on this project.

## Format

YYYY-MM-DD - <LLM_NAME> - <Brief summary> - Files: [list] - Version impact: <yes/no + details>

## Entries

- 2026-05-03 - Claude Opus 4.7 (1M context) + GPT-5 (concur) + Carlos (arbiter) - **Adopted LLM-DocKit 4.7.0 SessionStart-side enforcement primitive directly (no version bump in this repo).** Trigger: a 2026-05-03 Codex CLI session inside `home-infra-protocol` opened to audit the freshly-landed `DEPLOYMENT_EVIDENCE_PROPOSAL` acceptance gave a partial ecosystem opinion because the agent had not read `LLM_START_HERE.md` despite the rule being declared at lines 9 and 87 of that file. Diagnosis: passive instructions in repo docs depend on LLM discipline alone — the same family the operator's `~/.claude/CLAUDE.md` "Before adding a passive rule" section already names empirically. Cross-LLM concurrence (Carlos arbiter, Claude proposer, GPT-5 concur) on shipping a mechanical cure rather than more prose. **What landed in tomatic**: (1) `scripts/dockit-bootstrap-context.sh` — POSIX shell, ~7 KB, zero external deps, copied directly from LLM-DocKit 4.7.0 (registered in `dockit-sync-manifest.yml` upstream with `strategy: copy`, so future `dockit-sync` passes are no-ops here). The script reads this repo's `LLM_START_HERE.md` "Recommended reading order:" section dynamically and emits a Claude Code `additionalContext` JSON payload (`--json`, default) or plain text (`--human`) for non-Claude LLMs. Tomatic's reading-order has 9 entries (the standard 7 plus the source-design `Tomatic_v3_2.docx` and `AGENTS.md`); script smoke-tested at ~2.4 KB JSON output, validated with `python3 -m json.tool`, well under the 10 KB SessionStart hook limit. (2) `.claude/settings.json` — added a SessionStart hook block calling the script with `--json`, wrapped in `sh -c 'if [ -x scripts/dockit-bootstrap-context.sh ]; then ... ; fi'` for graceful degradation. The protocol enforced: first substantive reply must begin with literally `Onboarding loaded.` (after reading the listed files) or `Onboarding skipped: <reason>` (for trivial requests that do not depend on architectural context). The string is short and observable in transcripts — operator can spot agents that skip silently. Existing Stop / PostToolUse / PreCompact hooks unchanged. **What does NOT change**: tomatic stays at version 0.1.5 (no application-code change; the two new files are LLM-DocKit-managed scaffold paths excluded from per-commit version-bump enforcement, consistent with how `dockit-validate-session.sh` is treated). Pre-H0 status unchanged: `esphome-builder` still up at `http://10.0.0.220:6052/`. **Upstream context**: LLM-DocKit cut v4.6.1 → v4.7.0 (minor) shipping the primitive; DF-033 in `LLM-DocKit/docs/DOWNSTREAM_FEEDBACK.md` captures the failure mode at `Status: implemented` (Claude Code axis); D-007 records the precedent that future "always read X at session start" rules ship as a hook + script, not as more prose. Inverse counterpart of D-005 / `dockit-validate-session.sh` (session-end enforcement); together they bracket the session. - Files: [scripts/dockit-bootstrap-context.sh, .claude/settings.json, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 7 doc-version html-comment markers, CHANGELOG.md] - Cross-repo: LLM-DocKit cut v4.7.0 (script + manifest entry + settings.json template + DF-033 + D-007); operator-driven `dockit-sync` will close the same failure mode in `plaud-mirror`, `home-infra-protocol`, `infra-portal`, `llm-council` on next pass. - Version impact: yes (0.1.5 → 0.1.6, patch) — pre-commit hook correctly flagged the new SessionStart hook + script as code/config requiring a bump; my earlier "no bump" claim was incorrect. The hook also revealed a side bug: its exclusion regex `'^\\.claude/'` has a stray double-backslash and never matches; captured as follow-up, not addressed in this commit.
- 2026-05-03 - Claude Opus 4.7 (1M context) - **Audit follow-up after Pre-H0**: three fixes + two protocol-level lessons captured. (1) Renamed `tomatic-esphome` → `esphome-builder` in container_name, compose dir on NAS (`/share/Container/compose/tomatic/` → `/share/Container/compose/esphome-builder/`), and `home-infra/catalog/services.yml` id, tags, description. Reason: ESPHome Builder is a generic ESP32 dev tool; first-adopter project names belong in the description, not in the service id (cf. mosquitto). D-011 addendum records the rename and the audit signal. (2) Synced `infra.contract.yml`: removed the planned `tomatic-esphome-builder` from `services:` (not owned by tomatic), added new `consumes:` block listing `mosquitto` and `esphome-builder` as shared dependencies referencing back to `home-infra/catalog/services.yml`; expanded `services:` with the planned `tomatic-bridge` (H1) and `tomatic-control-core` (H4); added a comment explaining the `services:`-vs-`consumes:` distinction. (3) Synced `LLM_START_HERE.md` Current Focus to match HANDOFF (was still saying "Initial scaffold + doc rewrite"). Verified live at 14:30 UTC: catalog has `esphome-builder` (not `tomatic-esphome`), `state=up`, HTTP 200. Two cross-repo lessons from the audit: (a) `home-infra-protocol/docs/DOWNSTREAM_FEEDBACK.md` DF-003 added — "Consumer support matrix conflates repo HEAD with deployed version" (the audit found `infra-portal` repo at 0.8.0 supporting `interface` while production at 0.7.2 still doesn't, but the matrix would imply the support is live). (b) `LLM-DocKit/docs/DOWNSTREAM_FEEDBACK.md` DF-NNN added — "Validator PASS + commit pushed ≠ deployed" (LLM-DocKit measures internal repo discipline but has no signal that links the repo VERSION to the version actually running on the target host). Both DF entries are `open` with options for the next session to evaluate. Bumped 0.1.4 → 0.1.5 (patch: doc fix + infra.contract sync, no application code change). - Files: [infra.contract.yml, LLM_START_HERE.md, docs/llm/DECISIONS.md (D-011 addendum), docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION + 7 doc-version targets, /share/Container/compose/esphome-builder/docker-compose.yml on NAS] - Cross-repo: home-infra commit `15c0a1c`, home-infra-protocol DF-003, LLM-DocKit DF-NNN. - Version impact: yes (0.1.4 -> 0.1.5)
- 2026-05-02 - Claude Opus 4.7 (1M context) - **Pre-H0 deploy: ESPHome Builder live on NAS.** Pulled out of source-doc roadmap H10 to validate the NAS deploy chain early and unblock YAML exploration before firmware work. Concrete actions: (1) Created `/share/Container/compose/tomatic/{firmware/esphome,docker-compose.yml}` on NAS via ssh + scp. (2) Single-service compose with `image: ghcr.io/esphome/esphome:latest`, `network_mode: host`, mounted `firmware/esphome:/config` + `/etc/localtime:/etc/localtime:ro`, `restart: unless-stopped`. **`privileged: true` removed** from the source-doc compose because the NAS never holds USB (first flash is from operator's PC via web.esphome.io per source-doc ch. 12). (3) `docker-compose up -d` via the QNAP plugin path; container `tomatic-esphome` came up healthy. `curl http://10.0.0.220:6052/` returns HTTP 200 from dev-vm. (4) Registered `tomatic-esphome` in `home-infra/catalog/services.yml` with `category: tools`, `interface: web` (using `home-infra-protocol` 0.2.0's just-shipped field — first non-template adopter of `interface`), `status.type: http`, full description naming D-011 as the rationale. (5) Updated `home-infra/docs/{INVENTORY,SERVICES,PROJECTS}.md` (NAS containers tables, tomatic version 0.1.2 → 0.1.4 status `Pre-H0 (ESPHome Builder live)`). (6) `infra-portal/scripts/sync-catalog-to-nas.sh` pushed home-infra HEAD `87400d8` to the runtime catalog; portal reload picked it up; `/api/status` for `tomatic-esphome` returned `state=up` HTTP 200 at 23:28 UTC. (7) Wrote D-011 in this repo capturing context, decision, implementation deltas vs source-doc, and consequences. The diff vs source-doc is intentional and documented: only the *provisioning* of the Builder moves to Pre-H0; the real `tomatic-esp32.yaml`, first USB flash, and pump acceptance test stay at H10. The compose file is single-service today and grows additively from H1+. - Files: [home-infra/catalog/services.yml, home-infra/docs/INVENTORY.md, home-infra/docs/SERVICES.md, home-infra/docs/PROJECTS.md, /share/Container/compose/tomatic/docker-compose.yml on NAS, docs/llm/DECISIONS.md (+D-011), docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION + 7 doc-version targets] - Version impact: yes (0.1.3 -> 0.1.4)
- 2026-05-02 - Claude Opus 4.7 - MQTT broker decision (D-010 / home-infra ADR-0011). Carlos resolved Open Question 1 in favor of reusing the existing shared Mosquitto on the `zigbee` RPi instead of spinning a Tomatic-dedicated broker on the NAS. Wrote D-010 in this repo and ADR-0011 in home-infra; updated `home-infra/catalog/services.yml` to declare `mosquitto` as a first-class shared service (host `zigbee`, category `infra`, TCP status probe, deps wired into `zigbee2mqtt` and `ha`) so `infra-portal` can render it; updated `infra.contract.yml` with the planned ACL topic shapes and `tomatic_bridge` user; corrected `ARCHITECTURE.md` deployment table + storage layout + MQTT ACL section; corrected `STRUCTURE.md` NAS-only data list; amended D-004 NAS-first to point at the resolved broker decision; closed Open Questions 1 and 4 in HANDOFF.md. - Files: [home-infra/catalog/services.yml, home-infra/docs/DECISIONS.md, home-infra/HANDOFF.md, home-infra/docs/SERVICES.md, infra.contract.yml, docs/ARCHITECTURE.md, docs/STRUCTURE.md, docs/llm/DECISIONS.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-02 - Claude Opus 4.7 - Pre-H0 context audit. Verified live homelab MQTT state via SSH: NAS Mosquitto and MariaDB containers are REMOVED (not just stopped as docs claimed), only orphan compose dirs remain; the only live MQTT broker is on `zigbee` RPi 10.0.0.139. Corrected `home-infra/docs/{INVENTORY,SERVICES,PROJECTS}.md` (mosquitto/mariadb status, tomatic version 0.1.0->0.1.2, source doc path now points inside the repo). Updated `docs/llm/HANDOFF.md` Open Question 1 with the two real Mosquitto options for Tomatic now that the NAS broker is gone. - Files: [home-infra/docs/INVENTORY.md, home-infra/docs/SERVICES.md, home-infra/docs/PROJECTS.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-02 - Codex GPT-5 - Moved the canonical Tomatic Word source document into `docs/reference/`, added a reference index, updated documentation links to the in-repo copy, and bumped doc markers to 0.1.2. - Files: [docs/reference/Tomatic_v3_2.docx, docs/reference/README.md, README.md, LLM_START_HERE.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/STRUCTURE.md, docs/llm/HANDOFF.md, docs/llm/DECISIONS.md, CHANGELOG.md, VERSION] - Version impact: yes (0.1.1 -> 0.1.2)
- 2026-05-02 - LLM-DocKit init - Initial scaffold from LLM-DocKit 4.6.1. Conversation language: Spanish. - Files: [* (initial scaffold)] - Version impact: yes (initial 0.1.0)
- 2026-05-02 - Homelab profile apply - Added AGENTS.md, CLAUDE.md symlink, infra.contract.yml, .claude/checklists/homelab-project.md. - Files: [AGENTS.md, CLAUDE.md, infra.contract.yml, .claude/checklists/homelab-project.md] - Version impact: no
- 2026-05-02 - Claude Opus 4.7 - First substantive doc rewrite from `~/src/Tomatic_v3_2.docx`: README overview + Tomatic positioning; PROJECT_CONTEXT vision/objectives/stakeholders/components/milestones; ARCHITECTURE with 8 layers, hard rules R1–R12, key flows, MQTT topics excerpt, command lifecycle, MCP tool surface, deployment topology, V1.0–V1.2 roadmap; STRUCTURE with planned monorepo layout; HANDOFF set to H0 — Schemas + DB; DECISIONS pre-populated with D-001..D-009 from source doc §20; CHANGELOG entry; infra.contract.yml updated with real values where known. - Files: [README.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/STRUCTURE.md, docs/llm/HANDOFF.md, docs/llm/DECISIONS.md, docs/llm/HISTORY.md, CHANGELOG.md, LLM_START_HERE.md, infra.contract.yml] - Version impact: no (stays at 0.1.0; only the doc-version markers move when scripts/bump-version.sh runs)
